layui.use('laydate', function () {
    var laydate = layui.laydate;
    laydate.render({
        elem: '#date1'
        , type: 'datetime'
    });

    laydate.render({
        elem: '#date2'
        , type: 'datetime'
    });

    laydate.render({
        elem: '#date3'
        , type: 'datetime'
    });

    laydate.render({
        elem: '#date4'
        , type: 'datetime'
    });

    laydate.render({
        elem: '#date5'
        , type: 'datetime'
    });

    laydate.render({
        elem: '#date6'
        , type: 'datetime'
    });

});



